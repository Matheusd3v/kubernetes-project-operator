apiVersion: projects.home.lab/v1
kind: Project
metadata:
  labels:
    app.kubernetes.io/name: kubernetes-project-operator
    app.kubernetes.io/managed-by: kustomize
  name: project-sample
spec:
  # TODO(user): Add fields here
